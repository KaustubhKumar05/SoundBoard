{
  "name": "soundboard-server",
  "version": "1.0.0",
  "description": "Backend for Soundboard",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.20.0",
    "redis": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
